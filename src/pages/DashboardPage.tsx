import { motion } from 'framer-motion'
import { useAuth } from '../context/AuthContext'
const descriptions = { student: 'Your secure space for campus support and grievance updates.', staff: 'Your workspace for supporting the university community.', admin: 'Your central workspace for university service management.' }
export default function DashboardPage() { const { user } = useAuth(); const role = user?.role ?? 'student'; return <motion.div initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }}><p className="text-sm font-medium capitalize text-brand-600">{role} portal</p><h1 className="mt-1 text-3xl font-bold">Welcome, {user?.name}</h1><article className="mt-8 max-w-2xl rounded-2xl border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900"><h2 className="text-lg font-semibold">Your dashboard is ready</h2><p className="mt-2 text-slate-600 dark:text-slate-300">{descriptions[role]}</p><p className="mt-5 rounded-lg bg-brand-50 p-4 text-sm text-brand-700 dark:bg-brand-500/10 dark:text-brand-50">More dashboard tools will arrive in the next ShikshaDost phase.</p></article></motion.div> }
