import { ArrowLeftIcon } from '@heroicons/react/24/outline'
import { useQuery } from '@tanstack/react-query'
import { Link, useParams } from 'react-router-dom'
import { administrationService } from '../../services/administration'

export default function StaffDetailsPage() { const { staffId = '' } = useParams(); const { data, isLoading } = useQuery({ queryKey: ['staff-member', staffId], queryFn: () => administrationService.staffMember(staffId) }); if (isLoading) return <p className="text-slate-500">Loading staff profile...</p>; if (!data) return <p className="text-rose-600">Staff member not found.</p>; return <div className="mx-auto max-w-3xl"><Link to="/admin/staff" className="mb-5 inline-flex items-center gap-2 text-sm font-semibold text-slate-500 hover:text-brand-600"><ArrowLeftIcon className="h-4 w-4" />All staff</Link><section className="rounded-2xl border border-slate-200 bg-white p-7 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p className="text-sm font-semibold text-brand-600">Staff profile</p><h1 className="mt-1 text-3xl font-bold">{data.name}</h1><p className="mt-2 text-slate-500">{data.email}</p><dl className="mt-7 grid gap-5 sm:grid-cols-2"><div><dt className="text-sm text-slate-500">Department</dt><dd className="mt-1 font-semibold">{data.department || 'Unassigned'}</dd></div><div><dt className="text-sm text-slate-500">Account status</dt><dd className="mt-1 font-semibold">{data.isActive ? 'Active' : 'Disabled'}</dd></div><div><dt className="text-sm text-slate-500">Phone</dt><dd className="mt-1 font-semibold">{data.phone || 'Not provided'}</dd></div><div><dt className="text-sm text-slate-500">Joined</dt><dd className="mt-1 font-semibold">{new Date(data.createdAt).toLocaleDateString()}</dd></div></dl></section></div> }
