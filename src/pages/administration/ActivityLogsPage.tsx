import { useQuery } from '@tanstack/react-query'
import { useState } from 'react'
import { administrationService } from '../../services/administration'

export default function ActivityLogsPage() { const [search, setSearch] = useState(''); const [module, setModule] = useState(''); const { data, isLoading } = useQuery({ queryKey: ['activity-logs', search, module], queryFn: () => administrationService.logs({ search, module, pageSize: 100 }) }); return <div><h1 className="text-3xl font-bold tracking-tight">Activity logs</h1><p className="mt-2 text-slate-500">A tamper-evident trail of administration actions.</p><div className="mt-7 flex flex-col gap-3 sm:flex-row"><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search actions or users" className="flex-1 rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm dark:border-slate-700 dark:bg-slate-900" /><select value={module} onChange={(e) => setModule(e.target.value)} className="rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm dark:border-slate-700 dark:bg-slate-900"><option value="">All modules</option><option>Departments</option><option>Staff</option><option>Complaints</option></select></div><div className="mt-5 overflow-hidden rounded-2xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900">{isLoading ? <p className="p-10 text-center text-slate-500">Loading activity...</p> : data?.items.length ? data.items.map((item) => <div key={item.id} className="flex flex-col gap-2 border-b border-slate-100 p-5 last:border-0 sm:flex-row sm:items-center sm:justify-between dark:border-slate-800"><div><p className="font-semibold">{item.action} <span className="font-normal text-slate-500">in {item.module}</span></p><p className="mt-1 text-sm text-slate-600 dark:text-slate-300">{item.description}</p><p className="mt-1 text-xs text-slate-500">{item.userName || 'System'}</p></div><time className="text-xs text-slate-500">{new Date(item.createdAt).toLocaleString()}</time></div>) : <p className="p-10 text-center text-sm text-slate-500">No activity has been recorded yet.</p>}</div></div> }
