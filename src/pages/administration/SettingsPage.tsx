import { ShieldCheckIcon } from '@heroicons/react/24/outline'
const roles = [{ name: 'Student', permissions: 'Create complaints and view only their own records.' }, { name: 'Staff', permissions: 'View department-assigned complaints, update status, and add remarks.' }, { name: 'Administrator', permissions: 'Manage users, staff, departments, complaints, analytics, reports, notifications, and logs.' }]
export default function SettingsPage() { return <div className="mx-auto max-w-4xl"><h1 className="text-3xl font-bold tracking-tight">Settings & roles</h1><p className="mt-2 text-slate-500">Role-based access controls are enforced server-side for every administrative API.</p><section className="mt-7 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex items-center gap-3"><div className="rounded-xl bg-brand-50 p-2.5 text-brand-600 dark:bg-brand-500/10"><ShieldCheckIcon className="h-6 w-6" /></div><div><h2 className="font-bold">Role management</h2><p className="text-sm text-slate-500">System roles are intentionally fixed in Phase 1 to maintain auditability.</p></div></div><div className="mt-6 divide-y divide-slate-100 dark:divide-slate-800">{roles.map((role) => <div key={role.name} className="py-5"><h3 className="font-bold">{role.name}</h3><p className="mt-1 text-sm leading-6 text-slate-600 dark:text-slate-300">{role.permissions}</p></div>)}</div></section></div> }
