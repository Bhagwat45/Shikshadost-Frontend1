import { ArrowLeftIcon } from '@heroicons/react/24/outline'
import { useQuery } from '@tanstack/react-query'
import { Link, useParams } from 'react-router-dom'
import { administrationService } from '../../services/administration'

export default function DepartmentDetailsPage() { const { departmentId = '' } = useParams(); const { data, isLoading } = useQuery({ queryKey: ['department', departmentId], queryFn: () => administrationService.department(departmentId) }); if (isLoading) return <p className="text-slate-500">Loading department...</p>; if (!data) return <p className="text-rose-600">Department not found.</p>; return <div className="mx-auto max-w-3xl"><Link to="/admin/departments" className="mb-5 inline-flex items-center gap-2 text-sm font-semibold text-slate-500 hover:text-brand-600"><ArrowLeftIcon className="h-4 w-4" />All departments</Link><section className="rounded-2xl border border-slate-200 bg-white p-7 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p className="text-sm font-semibold text-brand-600">Department profile</p><h1 className="mt-1 text-3xl font-bold">{data.departmentName}</h1><p className="mt-5 leading-7 text-slate-600 dark:text-slate-300">{data.description}</p><dl className="mt-7 grid gap-5 sm:grid-cols-2"><div><dt className="text-sm text-slate-500">Department email</dt><dd className="mt-1 font-semibold">{data.email}</dd></div><div><dt className="text-sm text-slate-500">Head of department</dt><dd className="mt-1 font-semibold">{data.headOfDepartment || 'Not assigned'}</dd></div><div><dt className="text-sm text-slate-500">Staff members</dt><dd className="mt-1 font-semibold">{data.staffMembers.length}</dd></div><div><dt className="text-sm text-slate-500">Complaint resolution</dt><dd className="mt-1 font-semibold">{data.resolvedCount} of {data.complaintCount} resolved</dd></div></dl></section></div> }
