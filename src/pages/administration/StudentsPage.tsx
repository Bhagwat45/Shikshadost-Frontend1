import { AcademicCapIcon } from '@heroicons/react/24/outline'
import { useQuery } from '@tanstack/react-query'
import { useState } from 'react'
import { administrationService } from '../../services/administration'

export default function StudentsPage() { const [search, setSearch] = useState(''); const { data, isLoading } = useQuery({ queryKey: ['students', search], queryFn: () => administrationService.students({ search, pageSize: 100 }) }); return <div><h1 className="text-3xl font-bold tracking-tight">Students</h1><p className="mt-2 text-slate-500">Search registered student accounts and review their access status.</p><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search students by name or email" className="mt-7 w-full max-w-xl rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm dark:border-slate-700 dark:bg-slate-900" /><div className="mt-5 overflow-hidden rounded-2xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900">{isLoading ? <p className="p-10 text-center text-slate-500">Loading students...</p> : data?.items.length ? data.items.map((student) => <div key={student.id} className="flex items-center gap-4 border-b border-slate-100 p-5 last:border-0 dark:border-slate-800"><div className="rounded-xl bg-sky-50 p-2.5 text-sky-600 dark:bg-sky-500/10"><AcademicCapIcon className="h-5 w-5" /></div><div className="min-w-0 flex-1"><p className="font-semibold">{student.name}</p><p className="truncate text-sm text-slate-500">{student.email}</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${student.isActive ? 'bg-emerald-100 text-emerald-700 dark:bg-emerald-500/15 dark:text-emerald-300' : 'bg-slate-100 text-slate-600 dark:bg-slate-800'}`}>{student.isActive ? 'Active' : 'Disabled'}</span><span className="hidden text-xs text-slate-500 sm:block">Joined {new Date(student.createdAt).toLocaleDateString()}</span></div>) : <p className="p-10 text-center text-sm text-slate-500">No students found.</p>}</div></div> }
